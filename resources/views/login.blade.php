<x-layout page="Do it: Login">
  <x-slot:btn>
            <a href="{{route('register')}}" class="btn btn-primary">
                   Cadastre-se
                </a>
    </x-slot:btn>
<a href="{{route('home')}}">
    ir para home </a>
</x-layout>
